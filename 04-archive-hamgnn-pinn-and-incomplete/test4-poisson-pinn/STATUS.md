# test4-poisson-pinn status

This folder is reserved for a harder Poisson PINN benchmark:

- PDE: 1D Poisson with high-frequency target
- acceptance rule: only keep this benchmark if `muon_ns_adamw` beats `adamw`

The benchmark package includes:

- `code/pinn_poisson/`
- `scripts/submit_poisson_pinn_a800.sbatch`

Results and analysis will be added after the completed server run.
