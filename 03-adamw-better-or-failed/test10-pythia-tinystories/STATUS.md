# test10-pythia-tinystories status

- benchmark type: non-HamGNN real-model LLM benchmark
- data policy: local TinyStories text only
- current state: scaffold copied from earlier Pythia benchmark and being hardened for server submission
- acceptance gate: requires original `muon_ns` to beat `adamw`
